import 'package:flutter/widgets.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:morse_code/ui/morse_input.dart';

void main() {
  // testWidgets("Render morse input form", (WidgetTester tester) async {
  //   await tester.pumpWidget(MorseInputForm());
  //   final inputFinder = find.byKey(Key("input"));
  //   expect(inputFinder, findsOneWidget);
  // });
  // testWidgets("Invalid character input", (WidgetTester tester) async {});
}
